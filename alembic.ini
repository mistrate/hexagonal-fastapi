# Required by the alembic CLI, but deliberately empty: the real configuration
# lives in pyproject.toml ([tool.alembic]), and the database URL is resolved in
# app/shell/database/migrations/env.py (defaulting to the app's SQLite file —
# pass another URL programmatically via app.shell.database.run_migrations).
[alembic]
